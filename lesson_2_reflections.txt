what happens when you initialise a repository? why do you need it?

when init a repository git initialize a empty reposirtory for version tracking of files. its empty at th e beginning. commit is not there by fefault and master is current displayed.

we need it to keep tracks of different verison of our files.
